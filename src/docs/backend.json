

{
  "entities": {
    "User": {
      "title": "User",
      "type": "object",
      "description": "Represents a user of the ClarityFlow application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user."
        },
        "email": {
          "type": "string",
          "description": "User's email address.",
          "format": "email"
        },
        "firstName": {
          "type": "string",
          "description": "User's first name."
        },
        "lastName": {
          "type": "string",
          "description": "User's last name."
        }
      },
      "required": [
        "id",
        "email"
      ]
    },
    "Tenant": {
      "title": "Tenant",
      "type": "object",
      "description": "Represents a tenant (business) within the multi-tenant application, owned by a user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the tenant."
        },
        "name": {
          "type": "string",
          "description": "Name of the tenant (business)."
        },
        "userId": {
          "type": "string",
          "description": "The ID of the user who owns this tenant."
        },
        "subscriptionStatus": {
            "type": "string",
            "enum": ["active", "inactive", "trialing", "past_due", "canceled"],
            "description": "The current subscription status of the tenant."
        },
        "subscriptionTier": {
            "type": "string",
            "enum": ["none", "pro"],
            "description": "The subscription tier of the tenant."
        },
        "queueSkipTimeMinutes": {
            "type": "number",
            "description": "Number of minutes a client has to claim their spot before being skipped."
        },
        "lateArrivalGracePeriod": {
            "type": "number",
            "description": "Grace period in minutes before an appointment is marked as late."
        },
        "autoCancelLateArrivals": {
            "type": "boolean",
            "description": "If true, automatically cancels appointments for clients who are late beyond the grace period."
        },
        "cancellationFee": {
            "type": "number",
            "description": "Fee charged for late cancellations."
        },
        "cancellationWindowHours": {
            "type": "number",
            "description": "The window in hours before an appointment that a client can cancel without a fee."
        },
        "noShowFee": {
            "type": "number",
            "description": "Fee charged for clients who do not show up for their appointment."
        },
        "cancellationPolicy": {
            "type": "string",
            "description": "Text of the cancellation policy."
        },
        "noShowPolicy": {
            "type": "string",
            "description": "Text of the no-show policy."
        },
        "lateArrivalPolicy": {
            "type": "string",
            "description": "Text of the late arrival policy."
        },
        "bookingSlotInterval": {
            "type": "number",
            "enum": [15, 30, 60],
            "description": "The time interval for available appointment slots on the public booking page."
        },
        "referrerReward": {
            "type": "number",
            "description": "Store credit amount given to the referrer for a successful referral."
        },
        "newClientDiscount": {
            "type": "number",
            "description": "Discount amount for the new client on their first service."
        },
        "smsNotificationMessage": {
            "type": "string",
            "description": "Customizable SMS message for walk-in notifications."
        }
      },
      "required": [
        "id",
        "name",
        "userId",
        "subscriptionStatus",
        "subscriptionTier"
      ]
    },
    "DayHours": {
        "title": "Day Hours",
        "type": "object",
        "description": "Operating hours for a single day of the week.",
        "properties": {
            "enabled": {
                "type": "boolean",
                "description": "Whether the business is open on this day."
            },
            "start": {
                "type": "string",
                "description": "Opening time in HH:mm format (24-hour)."
            },
            "end": {
                "type": "string",
                "description": "Closing time in HH:mm format (24-hour)."
            }
        },
        "required": ["enabled"]
    },
    "Staff": {
      "title": "Staff",
      "type": "object",
      "description": "Represents a staff member of a tenant.",
      "properties": {
        "id": { "type": "string" },
        "tenantId": { "type": "string" },
        "name": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "phone": { "type": "string" },
        "avatarUrl": { "type": "string", "format": "uri" },
        "bio": { "type": "string", "description": "A short biography for the staff member's public profile." },
        "specialties": { "type": "array", "items": { "type": "string" }, "description": "A list of specialties or skills to highlight." },
        "portfolioImageUrls": { "type": "array", "items": { "type": "string", "format": "uri" }, "description": "A gallery of images showcasing the staff member's work." },
        "instagramUrl": { "type": "string", "format": "uri", "description": "Link to Instagram profile."},
        "facebookUrl": { "type": "string", "format": "uri", "description": "Link to Facebook profile."},
        "tiktokUrl": { "type": "string", "format": "uri", "description": "Link to TikTok profile."},
        "twitterUrl": { "type": "string", "format": "uri", "description": "Link to Twitter/X profile."},
        "pinterestUrl": { "type": "string", "format": "uri", "description": "Link to Pinterest profile."},
        "youtubeUrl": { "type": "string", "format": "uri", "description": "Link to YouTube profile."},
        "portfolioUrl": { "type": "string", "format": "uri", "description": "Link to an external portfolio or website."},
        "yearsOfExperience": { "type": "number", "description": "The staff member's years of experience in the industry." },
        "clientCount": { "type": "number", "description": "The approximate number of clients the staff member has served." },
        "role": { "type": "string", "enum": ["admin", "staff"] },
        "payStructure": { "type": "string", "enum": ["commission", "hourly", "salary"] },
        "commissionRate": { "type": "number", "description": "Commission rate as a percentage (e.g., 40 for 40%)." },
        "hourlyRate": { "type": "number", "description": "Hourly pay rate." },
        "emergencyContact": {
            "type": "object",
            "properties": {
                "name": { "type": "string" },
                "relationship": { "type": "string" },
                "phone": { "type": "string" }
            }
        },
        "availability": {
            "type": "object",
            "description": "The weekly availability for this staff member, overriding business hours.",
            "properties": {
                "week": {
                    "type": "object",
                    "properties": {
                        "sunday": { "$ref": "#/entities/DayHours" },
                        "monday": { "$ref": "#/entities/DayHours" },
                        "tuesday": { "$ref": "#/entities/DayHours" },
                        "wednesday": { "$ref": "#/entities/DayHours" },
                        "thursday": { "$ref": "#/entities/DayHours" },
                        "friday": { "$ref": "#/entities/DayHours" },
                        "saturday": { "$ref": "#/entities/DayHours" }
                    }
                }
            }
        },
        "availabilityNotes": { "type": "string" },
        "preferences": { "type": "string" },
        "compliance": {
            "type": "object",
            "properties": {
                "licenseNumber": { "type": "string" },
                "licenseExpiry": { "type": "string", "format": "date" },
                "documentUrl": { "type": "string", "format": "uri" }
            }
        },
        "active": { "type": "boolean", "description": "Whether the staff member is currently active and schedulable." },
        "onBreak": { "type": "boolean", "description": "Whether the staff member is currently on a break." },
        "breakStartTime": { "type": "string", "format": "date-time", "description": "The timestamp of when the staff member started their current break." },
        "status": { "type": "string", "enum": ["idle", "busy"], "description": "The current working status of the staff member." },
        "lastServedTimestamp": { "type": "string", "format": "date-time", "description": "The timestamp of when the staff member last completed a service." },
        "skillSet": {
            "type": "array",
            "description": "A list of skills the staff member possesses, used for smart assignment.",
            "items": { "type": "string" }
        },
        "services": {
            "type": "array",
            "description": "A list of service IDs the staff member is qualified to perform.",
            "items": { "type": "string" }
        }
      },
      "required": ["id", "tenantId", "name", "email", "role", "payStructure"]
    },
    "ActivityLog": {
        "title": "Activity Log",
        "description": "An immutable record of staff activity like clock-in/out and breaks.",
        "type": "object",
        "properties": {
            "id": { "type": "string" },
            "staffId": { "type": "string" },
            "type": { "type": "string", "enum": ["clock_in", "clock_out", "break_start", "break_end"] },
            "timestamp": { "type": "string", "format": "date-time" },
            "durationMinutes": { "type": "number", "description": "Duration of the event in minutes (e.g., for breaks)." }
        },
        "required": ["id", "staffId", "type", "timestamp"]
    },
    "Service": {
      "title": "Service",
      "type": "object",
      "description": "Represents a service offered by a tenant.",
      "properties": {
        "id": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "imageUrl": {
          "type": "string",
          "format": "uri",
          "description": "URL for the primary service image."
        },
        "durationMinutes": {
          "type": "number"
        },
        "price": {
          "type": "number"
        },
        "category": { "type": "string", "description": "The category of the service (e.g., nails, hair)." },
        "requiredSkills": {
            "type": "array",
            "description": "A list of skills required to perform this service.",
            "items": { "type": "string" }
        },
        "requiredResourceIds": {
            "type": "array",
            "description": "A list of resource IDs required for this service.",
            "items": { "type": "string" }
        },
        "capacity": {
            "type": "number",
            "description": "The maximum number of clients that can book this service for the same time slot. Defaults to 1."
        },
        "costPerAttendee": {
          "type": "number",
          "description": "The cost of materials for a single attendee, used for group services/classes."
        },
        "fixedCost": {
          "type": "number",
          "description": "Any fixed costs associated with offering the service once (e.g., instructor pay for a class)."
        }
      },
      "required": [
        "id",
        "name",
        "durationMinutes",
        "price"
      ]
    },
    "Resource": {
      "title": "Resource",
      "type": "object",
      "description": "Represents a bookable asset, like a room or a specific piece of equipment.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "type": { "type": "string", "enum": ["room", "equipment"] },
        "capacity": { "type": "number", "description": "Number of clients this resource can accommodate at once." },
        "inventoryItemId": { "type": "string", "description": "The ID of the linked inventory item, if this is an equipment resource." }
      },
      "required": ["id", "name", "type"]
    },
    "Product": {
      "title": "Product",
      "type": "object",
      "description": "Represents a product used or sold by a tenant.",
      "properties": {
        "id": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "cost": {
          "type": "number"
        },
        "quantity": {
          "type": "number"
        }
      },
      "required": [
        "id",
        "name",
        "cost",
        "quantity"
      ]
    },
    "Client": {
      "title": "Client",
      "type": "object",
      "description": "Represents a client of a tenant.",
      "properties": {
        "id": {
          "type": "string"
        },
        "firstName": {
          "type": "string"
        },
        "lastName": {
          "type": "string"
        },
        "email": {
          "type": "string",
          "format": "email"
        },
        "phone": {
          "type": "string"
        },
        "address": {
            "type": "object",
            "properties": {
                "street": { "type": "string" },
                "city": { "type": "string" },
                "state": { "type": "string" },
                "zip": { "type": "string" },
                "country": { "type": "string" }
            }
        },
        "emergencyContact": {
            "type": "object",
            "properties": {
                "name": { "type": "string" },
                "relationship": { "type": "string" },
                "phone": { "type": "string" }
            }
        },
        "notes": {
          "type": "object",
          "description": "Consultation and general notes about the client.",
          "properties": {
            "goals": {
              "type": "string",
              "description": "Client's goals for their service."
            },
            "routine": {
              "type": "string",
              "description": "Client's current at-home routine and products used."
            },
            "history": {
              "type": "string",
              "description": "Client's past service history and experiences."
            },
            "general": {
              "type": "string",
              "description": "General miscellaneous notes about the client."
            }
          }
        },
        "referralCode": {
            "type": "string",
            "description": "Unique, customizable code for the client to share."
        },
        "referredBy": {
            "type": "string",
            "description": "The name of the client who referred this person."
        },
        "successfulReferrals": {
            "type": "array",
            "items": { "type": "string" },
            "description": "A list of names of clients this person has successfully referred."
        },
        "walletCredit": {
            "type": "number",
            "description": "The amount of store credit a client has, often earned via referrals."
        }
      },
      "required": [
        "id",
        "firstName",
        "lastName"
      ]
    },
    "Appointment": {
      "title": "Appointment",
      "type": "object",
      "description": "Represents an appointment booked with a tenant.",
      "properties": {
        "id": {
          "type": "string"
        },
        "clientId": {
          "type": "string"
        },
        "serviceId": {
          "type": "string"
        },
        "staffId": {
          "type": "string",
          "description": "The ID of the staff member performing the appointment."
        },
        "startTime": {
          "type": "string",
          "format": "date-time"
        },
        "endTime": {
          "type": "string",
          "format": "date-time"
        },
        "isWalkIn": {
            "type": "boolean",
            "description": "True if the appointment was created from the walk-in queue."
        },
        "actualStartTime": {
            "type": "string",
            "format": "date-time",
            "description": "The actual start time of the service, logged when staff begins."
        },
        "actualEndTime": {
            "type": "string",
            "format": "date-time",
            "description": "The actual end time of the service, logged when staff finishes."
        },
        "clientName": {
            "type": "string",
            "description": "Denormalized name of the client."
        },
        "clientEmail": {
            "type": "string",
            "description": "Denormalized email of the client, primarily for walk-ins."
        },
        "clientPhone": {
            "type": "string",
            "description": "Denormalized phone of the client, primarily for walk-ins."
        },
        "checkInStatus": {
            "type": "string",
            "enum": ["pending", "on_my_way", "arrived", "running_late"],
            "description": "The arrival status of the client for their appointment."
        },
        "checkInToken": {
            "type": "string",
            "description": "A unique, secure token for the client's check-in link."
        },
        "lateTimeMinutes": {
            "type": "number",
            "description": "The number of minutes the client has indicated they will be late."
        },
        "automatedRescheduleOffered": {
            "type": "boolean",
            "description": "Indicates if an automated reschedule option has been offered to a late client."
        },
        "requiredResourceIds": {
            "type": "array",
            "description": "A list of resource IDs required for this appointment.",
            "items": { "type": "string" }
        }
      },
      "required": [
        "id",
        "clientId",
        "serviceId",
        "startTime",
        "endTime"
      ]
    },
    "WalkIn": {
      "title": "Walk-In Queue Entry",
      "type": "object",
      "description": "Represents a single customer in the smart walk-in queue.",
      "properties": {
        "id": { "type": "string" },
        "clientId": { "type": "string", "description": "The ID of an existing client, if applicable." },
        "customerName": { "type": "string", "description": "The name of the walk-in customer." },
        "customerPhone": { "type": "string", "description": "Phone number for SMS updates." },
        "customerEmail": { "type": "string", "description": "Email address of the walk-in customer.", "format": "email" },
        "customerBirthday": { "type": "string", "description": "Birthday of the walk-in customer.", "format": "date" },
        "serviceIds": { "type": "array", "items": { "type": "string" } },
        "requiredSkills": { "type": "array", "items": { "type": "string" } },
        "estimatedDuration": { "type": "number", "description": "Total estimated minutes for all requested services." },
        "checkInTime": { "type": "string", "format": "date-time" },
        "notifiedTimestamp": { "type": "string", "format": "date-time", "description": "Timestamp for when the client was notified it's their turn." },
        "serviceStartTime": { "type": "string", "format": "date-time", "description": "The time the service actually began." },
        "serviceEndTime": { "type": "string", "format": "date-time", "description": "The time the service actually ended." },
        "status": { "type": "string", "enum": ["waiting", "notified", "assigned", "servicing", "completed", "skipped", "cancelled", "ready_for_checkout"] },
        "assignedStaffId": { "type": "string", "description": "The ID of the staff member assigned to this customer." },
        "notes": { "type": "string" },
        "preferredStaffId": { "type": "string" },
        "waitForPreferredStaff": { "type": "boolean", "description": "Whether the client wants to wait for their preferred staff member." }
      },
      "required": ["id", "customerName", "serviceIds", "checkInTime", "status"]
    },
    "Bill": {
      "title": "Bill",
      "type": "object",
      "description": "A recurring bill or expense for a tenant.",
      "properties": {
        "id": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "amount": {
          "type": "number"
        },
        "dueDay": {
          "type": "number"
        },
        "billingCycle": {
          "type": "string",
          "enum": ["monthly", "annually", "quarterly", "weekly"]
        },
        "context": {
          "type": "string",
          "enum": ["Business", "Personal"]
        },
        "category": {
          "type": "string"
        }
      },
      "required": ["id", "name", "amount", "dueDay", "billingCycle", "context", "category"]
    },
    "LifestyleProfile": {
      "title": "Lifestyle Profile",
      "type": "object",
      "description": "A financial profile representing a user's personal lifestyle costs.",
      "properties": {
        "id": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "isActive": {
          "type": "boolean"
        }
      },
      "required": ["id", "name", "isActive"]
    },
    "BusinessProfile": {
      "title": "Business Profile",
      "type": "object",
      "description": "A financial profile representing a tenant's business operating costs.",
      "properties": {
        "id": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "isActive": {
          "type": "boolean"
        }
      },
      "required": ["id", "name", "isActive"]
    },
    "ScheduleProfile": {
      "title": "Schedule Profile",
      "type": "object",
      "description": "A profile defining a tenant's work availability and schedule.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "isActive": { "type": "boolean" },
        "week": {
            "type": "object",
            "description": "The weekly operating hours.",
            "properties": {
                "sunday": { "$ref": "#/entities/DayHours" },
                "monday": { "$ref": "#/entities/DayHours" },
                "tuesday": { "$ref": "#/entities/DayHours" },
                "wednesday": { "$ref": "#/entities/DayHours" },
                "thursday": { "$ref": "#/entities/DayHours" },
                "friday": { "$ref": "#/entities/DayHours" },
                "saturday": { "$ref": "#/entities/DayHours" }
            }
        },
        "timeOff": {
            "type": "object",
            "properties": {
                "vacationDays": { "type": "number" },
                "holidays": { "type": "number" }
            }
        }
      },
      "required": ["id", "name", "isActive"]
    },
    "Transaction": {
      "title": "Transaction",
      "type": "object",
      "description": "Represents a single financial transaction (income, expense, etc.) for a tenant.",
      "properties": {
        "id": {
          "type": "string"
        },
        "date": {
          "type": "string",
          "format": "date-time"
        },
        "description": {
          "type": "string"
        },
        "clientOrVendor": {
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": ["income", "expense", "reversal", "payment"]
        },
        "context": {
          "type": "string",
          "enum": ["Business", "Personal"]
        },
        "category": {
          "type": "string"
        },
        "amount": {
          "type": "number"
        },
        "paymentMethod": {
          "type": "string",
          "description": "The method of payment used for the transaction."
        },
        "hasReceipt": {
          "type": "boolean"
        },
        "staffId": {
            "type": "string",
            "description": "The ID of the staff member associated with the transaction."
        },
        "tipAmount": {
            "type": "number",
            "description": "The tip amount included in this transaction."
        },
        "appointmentId": {
            "type": "string",
            "description": "The ID of the appointment this transaction is associated with."
        }
      },
      "required": ["id", "date", "description", "type", "context", "category", "amount", "paymentMethod"]
    },
    "StockCorrection": {
      "title": "Stock Correction",
      "type": "object",
      "description": "Represents a single atomic change to inventory stock levels.",
      "properties": {
        "id": { "type": "string" },
        "productId": { "type": "string", "description": "The ID of the product being adjusted." },
        "date": { "type": "string", "format": "date-time" },
        "change": { "type": "number", "description": "The amount of change (e.g., -20 for 20ml used, 1 for 1 new container)." },
        "unit": { "type": "string", "description": "The unit of the change (e.g., 'ml', 'uses', 'container')." },
        "reason": { "type": "string", "description": "The reason for the correction (e.g., 'Appointment #123', 'Manual Count', 'Spoilage')." }
      },
      "required": ["id", "productId", "date", "change", "unit", "reason"]
    },
    "Quote": {
      "title": "Quote",
      "type": "object",
      "description": "Represents a quote or proposal for a client event.",
      "properties": {
        "id": { "type": "string" },
        "clientId": { "type": "string" },
        "eventName": { "type": "string" },
        "eventDate": { "type": "string", "format": "date" },
        "eventLocation": { "type": "string" },
        "lineItems": {
          "type": "array",
          "items": { "type": "object" }
        },
        "travelExpenses": { "type": "number" },
        "projectFee": { "type": "number" },
        "notes": { "type": "string" },
        "totalHours": { "type": "number" },
        "status": { "type": "string", "enum": ["draft", "sent", "accepted", "declined", "booked"] },
        "createdAt": { "type": "string", "format": "date-time" },
        "userId": { "type": "string" }
      },
      "required": ["id", "clientId", "eventName", "status", "createdAt", "userId"]
    },
     "Event": {
      "title": "Event",
      "type": "object",
      "description": "Represents a calendar event, which could be a booked quote.",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "startTime": { "type": "string", "format": "date-time" },
        "endTime": { "type": "string", "format": "date-time" },
        "type": { "type": "string" },
        "location": { "type": "string" },
        "notes": { "type": "string" },
        "quoteId": { "type": "string", "description": "The ID of the quote this event was booked from." }
      },
      "required": ["id", "title", "startTime", "endTime"]
    },
    "ConsentForm": {
        "title": "Consent Form",
        "description": "A reusable consent form template (e.g., intake, waiver).",
        "type": "object",
        "properties": {
            "id": { "type": "string" },
            "title": { "type": "string" },
            "category": { "type": "string", "enum": ["Intake", "Waiver", "Release", "General"] },
            "isPasswordProtected": { "type": "boolean" },
            "notifyOnEdit": { "type": "boolean" },
            "fields": {
                "type": "array",
                "items": {
                    "$ref": "#/entities/FormField"
                }
            }
        },
        "required": ["id", "title", "category"]
    },
    "FormField": {
        "title": "Form Field",
        "description": "A single field within a consent form.",
        "type": "object",
        "properties": {
            "id": { "type": "string" },
            "type": { "type": "string", "enum": ["heading", "paragraph", "short-text", "long-text", "multiple-choice", "checkboxes", "image-upload", "signature"] },
            "label": { "type": "string" },
            "options": {
                "type": "array",
                "items": { "type": "string" }
            }
        },
        "required": ["id", "type", "label"]
    }
  },
  "auth": {
    "providers": [
      "password"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/User"
      },
      "description": "Stores user profiles."
    },
    "/tenants/{tenantId}": {
      "schema": {
        "$ref": "#/entities/Tenant"
      },
      "description": "Stores tenant (business) profiles. All business-specific data is nested under this."
    },
    "/appointmentCheckIns/{token}": {
        "schema": {
            "$ref": "#/entities/Appointment"
        },
        "description": "Publicly readable appointment data for check-in links. The document ID is the secret token."
    },
    "/tenants/{tenantId}/staff/{staffId}": {
      "schema": {
        "$ref": "#/entities/Staff"
      },
      "description": "Staff members belonging to a specific tenant."
    },
    "/tenants/{tenantId}/activityLogs/{logId}": {
        "schema": {
            "$ref": "#/entities/ActivityLog"
        },
        "description": "Immutable log of all staff activities for a tenant."
    },
    "/tenants/{tenantId}/services/{serviceId}": {
      "schema": {
        "$ref": "#/entities/Service"
      },
      "description": "Services offered by a specific tenant."
    },
    "/tenants/{tenantId}/resources/{resourceId}": {
      "schema": {
        "$ref": "#/entities/Resource"
      },
      "description": "Bookable assets like rooms or specific equipment."
    },
    "/tenants/{tenantId}/products/{productId}": {
      "schema": {
        "$ref": "#/entities/Product"
      },
      "description": "Products sold or used by a specific tenant."
    },
    "/tenants/{tenantId}/clients/{clientId}": {
      "schema": {
        "$ref": "#/entities/Client"
      },
      "description": "Clients belonging to a specific tenant."
    },
    "/tenants/{tenantId}/appointments/{appointmentId}": {
      "schema": {
        "$ref": "#/entities/Appointment"
      },
      "description": "Appointments for a specific tenant."
    },
    "/tenants/{tenantId}/walkIns/{walkInId}": {
      "schema": {
        "$ref": "#/entities/WalkIn"
      },
      "description": "The smart queue for walk-in customers."
    },
    "/tenants/{tenantId}/events/{eventId}": {
      "schema": {
        "$ref": "#/entities/Event"
      },
      "description": "Calendar events for a tenant, including booked quotes."
    },
    "/tenants/{tenantId}/quotes/{quoteId}": {
      "schema": {
        "$ref": "#/entities/Quote"
      },
      "description": "Quotes or proposals for a specific tenant."
    },
    "/tenants/{tenantId}/bills/{billId}": {
      "schema": {
        "$ref": "#/entities/Bill"
      },
      "description": "Recurring bills and expenses for a specific tenant."
    },
     "/tenants/{tenantId}/lifestyleProfiles/{profileId}": {
      "schema": {
        "$ref": "#/entities/LifestyleProfile"
      },
      "description": "Personal financial profiles for the user owning the tenant."
    },
    "/tenants/{tenantId}/businessProfiles/{profileId}": {
      "schema": {
        "$ref": "#/entities/BusinessProfile"
      },
      "description": "Business cost profiles for a specific tenant."
    },
    "/tenants/{tenantId}/scheduleProfiles/{profileId}": {
      "schema": {
        "$ref": "#/entities/ScheduleProfile"
      },
      "description": "Work schedule profiles for a specific tenant."
    },
    "/tenants/{tenantId}/transactions/{transactionId}": {
      "schema": {
        "$ref": "#/entities/Transaction"
      },
      "description": "Financial transaction log for a specific tenant."
    },
    "/tenants/{tenantId}/stockCorrections/{correctionId}": {
      "schema": {
        "$ref": "#/entities/StockCorrection"
      },
      "description": "An audit log of all inventory stock changes."
    },
    "/tenants/{tenantId}/consentForms/{formId}": {
      "schema": {
        "$ref": "#/entities/ConsentForm"
      },
      "description": "Stores consent form templates created by the tenant."
    }
  }
}

