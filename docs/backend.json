
{
  "entities": {
    "User": {
      "title": "User",
      "type": "object",
      "description": "Represents a user of the ClarityFlow application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user."
        },
        "email": {
          "type": "string",
          "description": "User's email address.",
          "format": "email"
        },
        "firstName": {
          "type": "string",
          "description": "User's first name."
        },
        "lastName": {
          "type": "string",
          "description": "User's last name."
        }
      },
      "required": [
        "id",
        "email"
      ]
    },
    "Tenant": {
      "title": "Tenant",
      "type": "object",
      "description": "Represents a tenant (business) within the multi-tenant application, owned by a user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the tenant."
        },
        "name": {
          "type": "string",
          "description": "Name of the tenant (business)."
        },
        "userId": {
          "type": "string",
          "description": "The ID of the user who owns this tenant."
        },
        "subscriptionStatus": {
            "type": "string",
            "enum": ["active", "inactive", "trialing", "past_due", "canceled"],
            "description": "The current subscription status of the tenant."
        },
        "subscriptionTier": {
            "type": "string",
            "enum": ["none", "pro"],
            "description": "The subscription tier of the tenant."
        },
        "queueSkipTimeMinutes": {
            "type": "number",
            "description": "Number of minutes a client has to claim their spot before being skipped."
        },
        "lateArrivalGracePeriod": {
            "type": "number",
            "description": "Grace period in minutes before an appointment is marked as late."
        },
        "lateInconveniencePremium": {
            "type": "number",
            "description": "Fixed premium added to the TMHR-based late fee calculation."
        },
        "autoCancelLateArrivals": {
            "type": "boolean",
            "description": "If true, automatically cancels appointments for clients who are late beyond the grace period."
        },
        "cancellationFee": {
            "type": "number",
            "description": "Base fee charged for late cancellations."
        },
        "cancellationWindowHours": {
            "type": "number",
            "description": "The window in hours before an appointment that a client can cancel without a fee."
        },
        "noShowFee": {
            "type": "number",
            "description": "Fee charged for clients who do not show up for their appointment."
        },
        "allowDiscountStacking": {
            "type": "boolean",
            "default": false,
            "description": "If true, multiple discount codes can be applied to a single transaction."
        },
        "cancellationPolicy": {
            "type": "string",
            "description": "Text of the cancellation policy."
        },
        "noShowPolicy": {
            "type": "string",
            "description": "Text of the no-show policy."
        },
        "lateArrivalPolicy": {
            "type": "string",
            "description": "Text of the late arrival policy."
        },
        "bookingSlotInterval": {
            "type": "number",
            "enum": [15, 30, 60],
            "description": "The time interval for available appointment slots on the public booking page."
        },
        "referrerReward": {
            "type": "number",
            "description": "Store credit amount given to the referrer for a successful referral."
        },
        "newClientDiscount": {
            "type": "number",
            "description": "Discount amount for the new client on their first service."
        },
        "smsNotificationMessage": {
            "type": "string",
            "description": "Customizable SMS message for walk-in notifications."
        },
        "twilioAccountSid": {
          "type": "string",
          "description": "The Account SID for the tenant's Twilio account."
        },
        "twilioAuthToken": {
          "type": "string",
          "description": "The Auth Token for the tenant's Twilio account."
        },
        "twilioPhoneNumber": {
          "type": "string",
          "description": "The Twilio phone number used for sending SMS messages."
        },
        "tmhr": {
          "type": "number",
          "description": "True Minimum Hourly Rate for dynamic fee calculations."
        },
        "bookingPageSettings": {
            "type": "object",
            "description": "Customization settings for the public booking page.",
            "properties": {
                "heroImageUrl": { "type": "string" },
                "heroTitle": { "type": "string" },
                "heroSubtitle": { "type": "string" },
                "welcomeMessage": { "type": "string" },
                "primaryColor": { "type": "string" },
                "showTeam": { "type": "boolean", "default": true },
                "showReviews": { "type": "boolean", "default": true },
                "showFaq": { "type": "boolean", "default": true },
                "showGallery": { "type": "boolean", "default": true },
                "showMemberships": { "type": "boolean", "default": true },
                "showPackages": { "type": "boolean", "default": true },
                "servicesSectionTitle": { "type": "string", "default": "The Menu" },
                "teamSectionTitle": { "type": "string", "default": "The Experts" },
                "faqSectionTitle": { "type": "string", "default": "Intel" },
                "reviewsSectionTitle": { "type": "string", "default": "Voices" },
                "gallerySectionTitle": { "type": "string", "default": "The Vibe" },
                "policiesSectionTitle": { "type": "string", "default": "The Standard" },
                "contactSectionTitle": { "type": "string", "default": "Connect" },
                "faqs": {
                    "type": "array",
                    "items": {
                        "type": "object",
                        "properties": {
                            "id": { "type": "string" },
                            "question": { "type": "string" },
                            "answer": { "type": "string" }
                        },
                        "required": ["id", "question", "answer"]
                    }
                },
                "gallery": {
                    "type": "array",
                    "items": {
                        "type": "object",
                        "properties": {
                            "id": { "type": "string" },
                            "url": { "type": "string" },
                            "caption": { "type": "string" }
                        },
                        "required": ["id", "url"]
                    }
                }
            }
        }
      },
      "required": [
        "id",
        "name",
        "userId",
        "subscriptionStatus",
        "subscriptionTier"
      ]
    },
    "DayHours": {
        "title": "Day Hours",
        "type": "object",
        "description": "Operating hours for a single day of the week.",
        "properties": {
            "enabled": {
                "type": "boolean",
                "description": "Whether the business is open on this day."
            },
            "start": {
                "type": "string",
                "description": "Opening time in HH:mm format (24-hour)."
            },
            "end": {
                "type": "string",
                "description": "Closing time in HH:mm format (24-hour)."
            }
        },
        "required": ["enabled"]
    },
    "StaffDirectoryEntry": {
        "title": "Staff Directory Entry",
        "type": "object",
        "description": "A top-level entry to map a staff member's auth UID to their tenant.",
        "properties": {
            "tenantId": {
                "type": "string",
                "description": "The ID of the tenant this staff member belongs to."
            },
            "role": {
                "type": "string",
                "enum": ["admin", "staff"],
                "description": "The role of the staff member."
            }
        },
        "required": ["tenantId", "role"]
    },
    "Staff": {
      "title": "Staff",
      "type": "object",
      "description": "Represents a staff member of a tenant.",
      "properties": {
        "id": { "type": "string" },
        "tenantId": { "type": "string" },
        "name": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "phone": { "type": "string" },
        "avatarUrl": { "type": "string", "format": "uri" },
        "pin": { "type": "string", "description": "Unique 4-digit PIN for sensitive overrides." },
        "bio": { "type": "string", "description": "A short biography for the staff member's public profile." },
        "specialties": { "type": "array", "items": { "type": "string" }, "description": "A list of specialties or skills to highlight." },
        "portfolioImageUrls": { "type": "array", "items": { "type": "string", "format": "uri" }, "description": "A gallery of images showcasing the staff member's work." },
        "instagramUrl": { "type": "string", "format": "uri", "description": "Link to Instagram profile."},
        "facebookUrl": { "type": "string", "format": "uri", "description": "Link to Facebook profile."},
        "tiktokUrl": { "type": "string", "format": "uri", "description": "Link to TikTok profile."},
        "twitterUrl": { "type": "string", "format": "uri", "description": "Link to Twitter/X profile."},
        "pinterestUrl": { "type": "string", "format": "uri", "description": "Link to Pinterest profile."},
        "youtubeUrl": { "type": "string", "format": "uri", "description": "Link to YouTube profile."},
        "portfolioUrl": { "type": "string", "format": "uri", "description": "Link to an external portfolio or website."},
        "yearsOfExperience": { "type": "number", "description": "The staff member's years of experience in the industry." },
        "clientCount": { "type": "number", "description": "The approximate number of clients the staff member has served." },
        "pricingTierId": {
            "type": "string",
            "description": "The ID of the pricing tier this staff member belongs to."
        },
        "role": { "type": "string", "enum": ["admin", "staff"] },
        "payStructure": { "type": "string", "enum": ["commission", "hourly", "salary"] },
        "payoutFrequency": { "type": "string", "enum": ["weekly", "bi-weekly"], "description": "The frequency of payouts for commission-based staff." },
        "commissionRate": { "type": "number", "description": "Commission rate as a percentage (e.g., 40 for 40%)." },
        "retailCommissionRate": { "type": "number", "description": "Commission rate on retail sales as a percentage." },
        "hourlyRate": { "type": "number", "description": "Hourly pay rate." },
        "emergencyContact": {
            "type": "object",
            "properties": {
                "name": { "type": "string" },
                "relationship": { "type": "string" },
                "phone": { "type": "string" }
            }
        },
        "availability": {
            "type": "object",
            "description": "The weekly availability for this staff member, overriding business hours.",
            "properties": {
                "week": {
                    "type": "object",
                    "properties": {
                        "sunday": { "$ref": "#/entities/DayHours" },
                        "monday": { "$ref": "#/entities/DayHours" },
                        "tuesday": { "$ref": "#/entities/DayHours" },
                        "wednesday": { "$ref": "#/entities/DayHours" },
                        "thursday": { "$ref": "#/entities/DayHours" },
                        "friday": { "$ref": "#/entities/DayHours" },
                        "saturday": { "$ref": "#/entities/DayHours" }
                    }
                }
            }
        },
        "availabilityNotes": { "type": "string" },
        "preferences": { "type": "string" },
        "compliance": {
            "type": "object",
            "properties": {
                "licenseNumber": { "type": "string" },
                "licenseExpiry": { "type": "string", "format": "date" },
                "documentUrl": { "type": "string", "format": "uri" }
            }
        },
        "active": { "type": "boolean", "description": "Whether the staff member is currently active and schedulable." },
        "showOnPublicPage": { "type": "boolean", "default": true, "description": "Whether this staff member is visible on the public booking page." },
        "onBreak": { "type": "boolean", "description": "Whether the staff member is currently on a break." },
        "breakStartTime": { "type": "string", "format": "date-time", "description": "The timestamp of when the staff member started their current break." },
        "status": { "type": "string", "enum": ["idle", "busy"], "description": "The current working status of the staff member." },
        "lastServedTimestamp": { "type": "string", "format": "date-time", "description": "The timestamp of when the staff member last completed a service." },
        "skillSet": {
            "type": "array",
            "description": "A list of skills the staff member possesses, used for smart assignment.",
            "items": { "type": "string" }
        },
        "services": {
            "type": "array",
            "description": "A list of service IDs the staff member is qualified to perform.",
            "items": { "type": "string" }
        }
      },
      "required": ["id", "tenantId", "name", "email", "role", "payStructure"]
    },
    "PricingTier": {
      "title": "Pricing Tier",
      "type": "object",
      "description": "A customizable skill level for staff that determines service pricing and duration.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string", "description": "The display name of the tier (e.g., 'Senior Stylist')." },
        "rank": { "type": "number", "description": "The rank of the tier for sorting purposes (lower is more junior)." }
      },
      "required": ["id", "name", "rank"]
    },
    "ServiceTier": {
      "title": "Service Tier",
      "type": "object",
      "description": "Defines the price and duration of a specific service for a given pricing tier.",
      "properties": {
        "tierId": { "type": "string", "description": "The ID of the pricing tier this applies to." },
        "price": { "type": "number" },
        "durationMinutes": { "type": "number" }
      },
      "required": ["tierId", "price", "durationMinutes"]
    },
    "ActivityLog": {
        "title": "Activity Log",
        "description": "An immutable record of staff activity like clock-in/out and breaks.",
        "type": "object",
        "properties": {
            "id": { "type": "string" },
            "staffId": { "type": "string" },
            "type": { "type": "string", "enum": ["clock_in", "clock_out", "break_start", "break_end"] },
            "timestamp": { "type": "string", "format": "date-time" },
            "durationMinutes": { "type": "number", "description": "Duration of the event in minutes (e.g., for breaks)." }
        },
        "required": ["id", "staffId", "type", "timestamp"]
    },
    "Service": {
      "title": "Service",
      "type": "object",
      "description": "Represents a service offered by a tenant.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "price": { "type": "number", "description": "The base or default price for the service." },
        "description": { "type": "string" },
        "imageUrl": {
          "type": "string",
          "format": "uri",
          "description": "URL for the primary service image."
        },
        "durationMinutes": { "type": "number", "description": "The base or default duration for the service in minutes." },
        "serviceTiers": {
            "type": "array",
            "description": "Tiered pricing and duration based on staff pricing tiers.",
            "items": {
                "$ref": "#/entities/ServiceTier"
            }
        },
        "category": { "type": "string", "description": "The category of the service (e.g., nails, hair)." },
        "requiredSkills": {
            "type": "array",
            "description": "A list of skills required to perform this service.",
            "items": { "type": "string" }
        },
        "requiredResourceIds": {
            "type": "array",
            "description": "A list of resource IDs required for this service.",
            "items": { "type": "string" }
        },
        "capacity": {
            "type": "number",
            "description": "The maximum number of clients that can book this service for the same time slot. Defaults to 1."
        },
        "costPerAttendee": {
          "type": "number",
          "description": "The cost of materials for a single attendee, used for group services/classes."
        },
        "fixedCost": {
          "type": "number",
          "description": "Any fixed costs associated with offering the service once (e.g., instructor pay for a class)."
        }
      },
      "required": [
        "id",
        "name"
      ]
    },
    "Resource": {
      "title": "Resource",
      "type": "object",
      "description": "Represents a bookable asset, like a room or a specific piece of equipment.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "type": { "type": "string", "enum": ["room", "equipment"] },
        "capacity": { "type": "number", "description": "Number of clients this resource can accommodate at once." },
        "inventoryItemId": { "type": "string", "description": "The ID of the linked inventory item, if this is an equipment resource." }
      },
      "required": ["id", "name", "type"]
    },
    "Product": {
      "title": "Product",
      "type": "object",
      "description": "Represents a product used or sold by a tenant.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "description": { "type": "string" },
        "cost": { "type": "number" },
        "quantity": { "type": "number" },
        "msrp": { "type": "number", "description": "Manufacturer's Suggested Retail Price (DTC Price)." },
        "wholesalePrice": { "type": "number", "description": "Price for wholesale transactions." },
        "packagingCost": { "type": "number", "description": "Cost of packaging per item for DTC sales." },
        "shippingCostToCustomer": { "type": "number", "description": "Average shipping cost per item for DTC sales." }
      },
      "required": [ "id", "name", "cost", "quantity" ]
    },
    "Client": {
      "title": "Client",
      "type": "object",
      "description": "Represents a client of a tenant.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "phone": { "type": "string" },
        "status": { "type": "string", "enum": ["active", "archived", "banned"] },
        "activeMembershipId": { "type": "string" },
        "subscription": {
          "type": "object",
          "properties": {
            "membershipId": { "type": "string" },
            "status": { "type": "string", "enum": ["active", "past_due", "canceled"] },
            "nextBillingDate": { "type": "string", "format": "date-time" },
            "perkLastUsed": { "type": "string", "format": "date-time" }
          }
        },
        "activePackages": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "packageId": { "type": "string" },
              "sessionsRemaining": { "type": "number" }
            },
            "required": ["packageId", "sessionsRemaining"]
          }
        },
        "address": {
            "type": "object",
            "properties": {
                "street": { "type": "string" },
                "city": { "type": "string" },
                "state": { "type": "string" },
                "zip": { "type": "string" },
                "country": { "type": "string" }
            }
        },
        "emergencyContact": {
            "type": "object",
            "properties": {
                "name": { "type": "string" },
                "relationship": { "type": "string" },
                "phone": { "type": "string" }
            }
        },
        "notes": {
          "type": "object",
          "description": "Consultation and general notes about the client.",
          "properties": {
            "goals": {
              "type": "string",
              "description": "Client's goals for their service."
            },
            "routine": {
              "type": "string",
              "description": "Client's current at-home routine and products used."
            },
            "history": {
              "type": "string",
              "description": "Client's past service history and experiences."
            },
            "general": {
              "type": "string",
              "description": "General miscellaneous notes about the client."
            }
          }
        },
        "referralCode": {
            "type": "string",
            "description": "Unique, customizable code for the client to share."
        },
        "referredBy": {
            "type": "string",
            "description": "The name of the client who referred this person."
        },
        "successfulReferrals": {
            "type": "array",
            "items": { "type": "string" },
            "description": "A list of names of clients this person has successfully referred."
        },
        "walletCredit": {
            "type": "number",
            "description": "The amount of store credit a client has, often earned via referrals."
        },
        "outstandingBalance": {
            "type": "number",
            "description": "The total amount of outstanding fees (e.g., from cancellations, no-shows)."
        },
        "unpaidFees": {
            "type": "array",
            "description": "A detailed list of individual unpaid fees.",
            "items": {
                "type": "object",
                "properties": {
                    "feeId": { "type": "string" },
                    "appointmentId": { "type": "string" },
                    "appointmentDate": { "type": "string", "format": "date-time" },
                    "feeAmount": { "type": "number" },
                    "reason": { "type": "string" }
                },
                "required": ["feeId", "appointmentId", "appointmentDate", "feeAmount", "reason"]
            }
        },
        "banReason": { "type": "string" },
        "bannedAt": { "type": "string", "format": "date-time" },
        "bannedBy": { "type": "string" }
      },
      "required": [
        "id",
        "name"
      ]
    },
    "Appointment": {
      "title": "Appointment",
      "type": "object",
      "description": "Represents an appointment booked with a tenant.",
      "properties": {
        "id": { "type": "string" },
        "tenantId": { "type": "string", "description": "The ID of the tenant this appointment belongs to." },
        "clientId": { "type": "string" },
        "serviceId": { "type": "string" },
        "staffId": { "type": "string", "description": "The ID of the staff member performing the appointment." },
        "startTime": { "type": "string", "format": "date-time" },
        "endTime": { "type": "string", "format": "date-time" },
        "status": { "type": "string", "enum": ["confirmed", "completed", "cancelled", "deposit_pending", "ready_for_checkout", "servicing"] },
        "source": { "type": "string", "enum": ["online", "walk-in", "manual"] },
        "isWalkIn": { "type": "boolean" },
        "actualStartTime": { "type": "string", "format": "date-time" },
        "actualEndTime": { "type": "string", "format": "date-time" },
        "clientName": { "type": "string" },
        "clientEmail": { "type": "string" },
        "clientPhone": { "type": "string" },
        "checkInStatus": { "type": "string", "enum": ["pending", "on_my_way", "arrived", "running_late", "auto_cancelled"] },
        "checkInToken": { "type": "string" },
        "lateTimeMinutes": { "type": "number" },
        "cancellationReason": { "type": "string", "enum": ["late", "no-show", "client_request", "other"] },
        "cancellationFeeApplied": { "type": "number" },
        "cancellationFeeWaived": { "type": "boolean" },
        "overrideReason": { "type": "string", "description": "The reason why an auto-cancellation was overridden." },
        "overriddenBy": { "type": "string", "description": "The ID of the staff member who performed the override." }
      },
      "required": [
        "id",
        "tenantId",
        "clientId",
        "serviceId",
        "startTime",
        "endTime",
        "status",
        "source"
      ]
    },
    "WalkIn": {
      "title": "Walk-In Queue Entry",
      "type": "object",
      "description": "Represents a single customer in the smart walk-in queue. Customers can be part of a group.",
      "properties": {
        "id": { "type": "string" },
        "groupId": { "type": "string", "description": "A unique identifier for the group this entry belongs to. For individuals, this is their own walk-in ID." },
        "groupName": { "type": "string", "description": "The display name for the group, e.g., 'Jane's Party'." },
        "isPrimaryContact": { "type": "boolean", "description": "True if this person is the primary contact for the group." },
        "clientId": { "type": "string", "description": "The ID of an existing client, if known." },
        "customerName": { "type": "string", "description": "The name of the walk-in customer." },
        "customerPhone": { "type": "string", "description": "Phone number for SMS updates (usually only for primary contact)." },
        "customerEmail": { "type": "string", "description": "Email address of the walk-in customer.", "format": "email" },
        "customerBirthday": { "type": "string", "description": "Birthday of the walk-in customer.", "format": "date" },
        "serviceIds": { "type": "array", "items": { "type": "string" }, "description": "Services requested by this customer." },
        "requiredSkills": { "type": "array", "items": { "type": "string" } },
        "estimatedDuration": { "type": "number", "description": "Estimated minutes for this customer's services." },
        "checkInTime": { "type": "string", "format": "date-time" },
        "notifiedTimestamp": { "type": "string", "format": "date-time", "description": "Timestamp for when the client was notified it's their turn." },
        "serviceStartTime": { "type": "string", "format": "date-time", "description": "The time the service actually began." },
        "serviceEndTime": { "type": "string", "format": "date-time", "description": "The time the service actually ended." },
        "status": { "type": "string", "enum": ["waiting", "notified", "assigned", "servicing", "completed", "skipped", "cancelled", "ready_for_checkout"] },
        "queueOrder": { "type": "number", "description": "A number used for manually reordering the queue." },
        "assignedStaffId": { "type": "string", "description": "The ID of the staff member assigned to this customer." },
        "notes": { "type": "string" },
        "preferredStaffId": { "type": "string" },
        "waitForPreferredStaff": { "type": "boolean", "description": "Whether the client wants to wait for their preferred staff member." }
      },
      "required": ["id", "groupId", "customerName", "serviceIds", "checkInTime", "status"]
    },
    "Order": {
      "title": "Order",
      "description": "Represents a purchase order for inventory supplies.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "supplier": { "type": "string" },
        "orderDate": { "type": "string", "format": "date-time" },
        "status": { "type": "string", "enum": ["Draft", "Placed", "Shipped", "Partially Received", "Received", "Cancelled"] },
        "trackingNumber": { "type": "string" },
        "trackingUrl": { "type": "string", "format": "uri" },
        "expectedArrivalDate": { "type": "string", "format": "date" },
        "items": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "productId": { "type": "string" },
              "productName": { "type": "string" },
              "quantity": { "type": "number" },
              "costPerUnit": { "type": "number" }
            },
            "required": ["productId", "productName", "quantity", "costPerUnit"]
          }
        },
        "notes": { "type": "string" },
        "invoiceUrl": { "type": "string", "format": "uri" },
        "paymentMethod": {
          "type": "string",
          "description": "The method of payment used for the purchase."
        },
        "paymentContext": {
          "type": "string",
          "enum": ["Business", "Personal"],
          "description": "The financial context of the payment."
        },
        "paymentMethodIdentifier": {
          "type": "string",
          "description": "An identifier for the payment method (e.g., last 4 digits of a card)."
        },
        "shippingCost": { "type": "number" },
        "taxCost": { "type": "number" },
        "discounts": { "type": "number" }
      },
      "required": ["id", "supplier", "orderDate", "status", "items"]
    },
    "Bill": {
      "title": "Bill",
      "type": "object",
      "description": "A recurring bill or expense for a tenant.",
      "properties": {
        "id": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "amount": {
          "type": "number"
        },
        "dueDay": {
          "type": "number"
        },
        "billingCycle": {
          "type": "string",
          "enum": ["monthly", "annually", "quarterly", "weekly"]
        },
        "context": {
          "type": "string",
          "enum": ["Business", "Personal"]
        },
        "category": {
          "type": "string"
        }
      },
      "required": ["id", "name", "amount", "dueDay", "billingCycle", "context", "category"]
    },
    "LifestyleProfile": {
      "title": "Lifestyle Profile",
      "type": "object",
      "description": "A financial profile representing a user's personal lifestyle costs.",
      "properties": {
        "id": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "isActive": {
          "type": "boolean"
        }
      },
      "required": ["id", "name", "isActive"]
    },
    "BusinessProfile": {
      "title": "Business Profile",
      "type": "object",
      "description": "A financial profile representing a tenant's business operating costs.",
      "properties": {
        "id": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "isActive": {
          "type": "boolean"
        }
      },
      "required": ["id", "name", "isActive"]
    },
    "ScheduleProfile": {
      "title": "Schedule Profile",
      "type": "object",
      "description": "A profile defining a tenant's work availability and schedule.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "isActive": { "type": "boolean" },
        "week": {
            "type": "object",
            "description": "The weekly operating hours.",
            "properties": {
                "sunday": { "$ref": "#/entities/DayHours" },
                "monday": { "$ref": "#/entities/DayHours" },
                "tuesday": { "$ref": "#/entities/DayHours" },
                "wednesday": { "$ref": "#/entities/DayHours" },
                "thursday": { "$ref": "#/entities/DayHours" },
                "friday": { "$ref": "#/entities/DayHours" },
                "saturday": { "$ref": "#/entities/DayHours" }
            }
        },
        "timeOff": {
            "type": "object",
            "properties": {
                "vacationDays": { "type": "number" },
                "holidays": { "type": "number" }
            }
        }
      },
      "required": ["id", "name", "isActive"]
    },
    "Transaction": {
      "title": "Transaction",
      "type": "object",
      "description": "Represents a single financial transaction (income, expense, etc.) for a tenant.",
      "properties": {
        "id": {
          "type": "string"
        },
        "date": {
          "type": "string",
          "format": "date-time"
        },
        "description": {
          "type": "string"
        },
        "clientOrVendor": {
          "type": "string"
        },
        "clientId": {
          "type": "string",
          "description": "The ID of the client this transaction is associated with."
        },
        "type": {
          "type": "string",
          "enum": ["income", "expense", "reversal", "payment"]
        },
        "context": {
          "type": "string",
          "enum": ["Business", "Personal"]
        },
        "category": {
          "type": "string"
        },
        "amount": {
          "type": "number"
        },
        "paymentMethod": {
          "type": "string",
          "description": "The method of payment used for the transaction."
        },
        "paymentMethodIdentifier": {
            "type": "string",
            "description": "An identifier for the payment method (e.g., last 4 digits of a card)."
        },
        "hasReceipt": {
          "type": "boolean"
        },
        "receiptUrl": {
          "type": "string",
          "format": "uri"
        },
        "staffId": {
            "type": "string",
            "description": "The ID of the staff member associated with the transaction."
        },
        "tipAmount": {
            "type": "number",
            "description": "The tip amount included in this transaction."
        },
        "appointmentId": {
            "type": "string",
            "description": "The ID of the appointment this transaction is associated with."
        },
        "relatedOrderId": {
            "type": "string",
            "description": "The ID of the purchase order this transaction is related to."
        },
        "reversalOf": {
            "type": "string",
            "description": "The ID of the transaction this entry is reversing."
        },
        "relatedBillInstanceId": {
            "type": "string",
            "description": "The ID of the bill instance this payment is for."
        }
      },
      "required": ["id", "date", "description", "type", "context", "category", "amount", "paymentMethod"]
    },
    "StockCorrection": {
      "title": "Stock Correction",
      "type": "object",
      "description": "Represents a single atomic change to inventory stock levels.",
      "properties": {
        "id": { "type": "string" },
        "productId": { "type": "string", "description": "The ID of the product being adjusted." },
        "date": { "type": "string", "format": "date-time" },
        "change": { "type": "number", "description": "The amount of change (e.g., -20 for 20ml used, 1 for 1 new container)." },
        "unit": { "type": "string", "description": "The unit of the change (e.g., 'ml', 'uses', 'container')." },
        "reason": { "type": "string", "description": "The reason for the correction (e.g., 'Appointment #123', 'Manual Count', 'Spoilage')." }
      },
      "required": ["id", "productId", "date", "change", "unit", "reason"]
    },
    "Quote": {
      "title": "Quote",
      "type": "object",
      "description": "Represents a quote or proposal for a client event.",
      "properties": {
        "id": { "type": "string" },
        "clientId": { "type": "string" },
        "eventName": { "type": "string" },
        "eventDate": { "type": "string", "format": "date" },
        "eventLocation": { "type": "string" },
        "lineItems": {
          "type": "array",
          "items": { "type": "object" }
        },
        "travelExpenses": { "type": "number" },
        "projectFee": { "type": "number" },
        "notes": { "type": "string" },
        "totalHours": { "type": "number" },
        "status": { "type": "string", "enum": ["draft", "sent", "accepted", "declined", "booked"] },
        "createdAt": { "type": "string", "format": "date-time" },
        "userId": { "type": "string" },
        "requiredFormIds": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Required consent forms attached to the quote."
        }
      },
      "required": ["id", "clientId", "eventName", "status", "createdAt", "userId"]
    },
     "Event": {
      "title": "Event",
      "type": "object",
      "description": "Represents a calendar event, which could be a booked quote.",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "startTime": { "type": "string", "format": "date-time" },
        "endTime": { "type": "string", "format": "date-time" },
        "type": { "type": "string" },
        "location": { "type": "string" },
        "notes": { "type": "string" },
        "staffIds": { "type": "array", "items": { "type": "string" }, "description": "The IDs of the staff members associated with this event." },
        "quoteId": { "type": "string", "description": "The ID of the quote this event was booked from." }
      },
      "required": ["id", "title", "startTime", "endTime"]
    },
    "ConsentForm": {
        "title": "Consent Form",
        "description": "A reusable consent form template (e.g., intake, waiver).",
        "type": "object",
        "properties": {
            "id": { "type": "string" },
            "title": { "type": "string" },
            "category": { "type": "string", "enum": ["Intake", "Waiver", "Release", "General"] },
            "isPasswordProtected": { "type": "boolean" },
            "notifyOnEdit": { "type": "boolean" },
            "fields": {
                "type": "array",
                "items": {
                    "$ref": "#/entities/FormField"
                }
            }
        },
        "required": ["id", "title", "category"]
    },
    "FormField": {
        "title": "Form Field",
        "description": "A single field within a consent form.",
        "type": "object",
        "properties": {
            "id": { "type": "string" },
            "type": { "type": "string", "enum": ["heading", "paragraph", "short-text", "long-text", "multiple-choice", "checkboxes", "image-upload", "signature"] },
            "label": { "type": "string" },
            "options": {
                "type": "array",
                "items": { "type": "string" }
            }
        },
        "required": ["id", "type", "label"]
    },
    "SignedConsent": {
        "title": "Signed Consent",
        "description": "A record of a consent form signed by a client.",
        "type": "object",
        "properties": {
            "id": { "type": "string" },
            "formId": { "type": "string", "description": "The ID of the ConsentForm template." },
            "formTitle": { "type": "string", "description": "The title of the form at the time of signing." },
            "clientId": { "type": "string" },
            "signedAt": { "type": "string", "format": "date-time" },
            "formData": {
                "type": "object",
                "description": "The data submitted by the client, where keys are field IDs and values are the answers."
            }
        },
        "required": ["id", "formId", "formTitle", "clientId", "signedAt"]
    },
    "Discount": {
      "title": "Discount",
      "type": "object",
      "description": "Represents a discount code that can be applied to sales.",
      "properties": {
        "id": { "type": "string" },
        "code": { "type": "string", "description": "The unique code customers will enter." },
        "description": { "type": "string", "description": "An internal description of the discount." },
        "type": { "type": "string", "enum": ["percentage", "fixed"], "description": "The type of discount." },
        "value": { "type": "number", "description": "The discount value (percentage or fixed amount)." },
        "usageLimit": { "type": "number", "description": "Total number of times the code can be used. 0 for unlimited." },
        "usageCount": { "type": "number", "description": "How many times the code has been used." },
        "isActive": { "type": "boolean", "description": "Whether the discount is currently active." },
        "validFrom": { "type": "string", "format": "date-time", "description": "The date the discount becomes valid." },
        "validUntil": { "type": "string", "format": "date-time", "description": "The date the discount expires." },
        "applicableServiceIds": {
            "type": "array",
            "items": { "type": "string" },
            "description": "A list of service IDs this discount applies to. If empty, it applies to the entire sale."
        },
        "limitOnePerCustomer": {
            "type": "boolean"
        },
        "usedByClientIds": {
            "type": "array",
            "items": { "type": "string" }
        },
        "automation": {
            "type": "object",
            "properties": {
                "trigger": {
                    "type": "string",
                    "enum": ["none", "new_client", "loyalty", "re_engagement", "birthday"],
                    "description": "The event that triggers this automation."
                },
                "appointmentThreshold": {
                    "type": "number",
                    "description": "Number of appointments to trigger loyalty discount."
                },
                "daysSinceLastVisit": {
                    "type": "number",
                    "description": "Number of days of inactivity to trigger re-engagement discount."
                }
            }
        }
      },
      "required": ["id", "code", "type", "value"]
    },
    "Campaign": {
      "title": "Campaign",
      "description": "Represents a marketing campaign to be sent to a specific client audience.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "subject": { "type": "string" },
        "subjectB": { "type": "string", "description": "An alternate subject line for A/B testing." },
        "body": { "type": "string" },
        "imageUrl": {
            "type": "string",
            "format": "uri",
            "description": "URL for an optional image to include in email campaigns."
        },
        "targetAudience": { "type": "string", "enum": ["all", "new", "loyal", "inactive_90", "specific", "birthday"] },
        "targetClientIds": {
            "type": "array",
            "items": { "type": "string" },
            "description": "A list of specific client IDs to target if audience is 'specific'."
        },
        "discountId": { "type": "string" },
        "status": { "type": "string", "enum": ["draft", "sent"] },
        "sentAt": { "type": "string", "format": "date-time" },
        "type": { "type": "string", "enum": ["email", "sms"] },
        "recipientCount": { "type": "number", "description": "The number of clients the campaign was sent to." },
        "openRate": { "type": "number", "description": "The percentage of recipients who opened the email." },
        "clickRate": { "type": "number", "description": "The percentage of recipients who clicked a link in the email." },
        "generatedRevenue": { "type": "number", "description": "The total revenue generated from this campaign." }
      },
      "required": ["id", "name", "type", "targetAudience", "status"]
    },
    "Review": {
      "title": "Review",
      "type": "object",
      "description": "A review submitted by a client for a service.",
      "properties": {
        "id": { "type": "string" },
        "tenantId": { "type": "string" },
        "clientId": { "type": "string" },
        "clientName": { "type": "string" },
        "clientAvatarUrl": { "type": "string", "format": "uri" },
        "staffId": { "type": "string" },
        "serviceId": { "type": "string" },
        "serviceName": { "type": "string" },
        "rating": { "type": "number" },
        "text": { "type": "string" },
        "isPublic": { "type": "boolean" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "tenantId", "clientId", "rating", "createdAt"]
    }
  },
  "auth": {
    "providers": [
      "password"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/User"
      },
      "description": "Stores user profiles."
    },
    "/tenants/{tenantId}": {
      "schema": {
        "$ref": "#/entities/Tenant"
      },
      "description": "Stores tenant (business) profiles. All business-specific data is nested under this."
    },
    "/appointmentCheckIns/{token}": {
        "schema": {
            "$ref": "#/entities/Appointment"
        },
        "description": "Publicly readable appointment data for check-in links. The document ID is the secret token."
    },
    "/tenants/{tenantId}/staff/{staffId}": {
      "schema": {
        "$ref": "#/entities/Staff"
      },
      "description": "Staff members belonging to a specific tenant."
    },
    "/tenants/{tenantId}/activityLogs/{logId}": {
        "schema": {
            "$ref": "#/entities/ActivityLog"
        },
        "description": "Immutable log of all staff activities for a tenant."
    },
    "/tenants/{tenantId}/services/{serviceId}": {
      "schema": {
        "$ref": "#/entities/Service"
      },
      "description": "Services offered by a specific tenant."
    },
    "/tenants/{tenantId}/resources/{resourceId}": {
      "schema": {
        "$ref": "#/entities/Resource"
      },
      "description": "Bookable assets like rooms or specific equipment."
    },
    "/tenants/{tenantId}/products/{productId}": {
      "schema": {
        "$ref": "#/entities/Product"
      },
      "description": "Products sold or used by a specific tenant."
    },
    "/tenants/{tenantId}/clients/{clientId}": {
      "schema": {
        "$ref": "#/entities/Client"
      },
      "description": "Clients belonging to a specific tenant."
    },
    "/tenants/{tenantId}/clients/{clientId}/signedConsents/{signedConsentId}": {
        "schema": { "$ref": "#/entities/SignedConsent" },
        "description": "Stores instances of consent forms signed by a specific client."
    },
    "/tenants/{tenantId}/appointments/{appointmentId}": {
      "schema": {
        "$ref": "#/entities/Appointment"
      },
      "description": "Appointments for a specific tenant."
    },
    "/tenants/{tenantId}/walkIns/{walkInId}": {
      "schema": {
        "$ref": "#/entities/WalkIn"
      },
      "description": "The smart queue for walk-in customers."
    },
    "/tenants/{tenantId}/events/{eventId}": {
      "schema": {
        "$ref": "#/entities/Event"
      },
      "description": "Calendar events for a tenant, including booked quotes."
    },
    "/tenants/{tenantId}/orders/{orderId}": {
        "schema": { "$ref": "#/entities/Order" },
        "description": "Purchase orders for inventory supplies."
    },
    "/tenants/{tenantId}/quotes/{quoteId}": {
      "schema": {
        "$ref": "#/entities/Quote"
      },
      "description": "Quotes or proposals for a specific tenant."
    },
    "/tenants/{tenantId}/bills/{billId}": {
      "schema": {
        "$ref": "#/entities/Bill"
      },
      "description": "Recurring bills and expenses for a specific tenant."
    },
     "/tenants/{tenantId}/lifestyleProfiles/{profileId}": {
      "schema": {
        "$ref": "#/entities/LifestyleProfile"
      },
      "description": "Personal financial profiles for the user owning the tenant."
    },
    "/tenants/{tenantId}/businessProfiles/{profileId}": {
      "schema": {
        "$ref": "#/entities/BusinessProfile"
      },
      "description": "Business cost profiles for a specific tenant."
    },
    "/tenants/{tenantId}/scheduleProfiles/{profileId}": {
      "schema": {
        "$ref": "#/entities/ScheduleProfile"
      },
      "description": "Work schedule profiles for a specific tenant."
    },
    "/tenants/{tenantId}/transactions/{transactionId}": {
      "schema": {
        "$ref": "#/entities/Transaction"
      },
      "description": "Financial transaction log for a specific tenant."
    },
    "/tenants/{tenantId}/stockCorrections/{correctionId}": {
      "schema": {
        "$ref": "#/entities/StockCorrection"
      },
      "description": "An audit log of all inventory stock changes."
    },
    "/tenants/{tenantId}/consentForms/{formId}": {
      "schema": {
        "$ref": "#/entities/ConsentForm"
      },
      "description": "Stores consent form templates created by the tenant."
    },
    "/tenants/{tenantId}/discounts/{discountId}": {
      "schema": {
        "$ref": "#/entities/Discount"
      },
      "description": "Stores discount codes for a specific tenant."
    },
    "/tenants/{tenantId}/campaigns/{campaignId}": {
      "schema": {
        "$ref": "#/entities/Campaign"
      },
      "description": "Stores marketing campaigns for a tenant."
    },
    "/tenants/{tenantId}/reviews/{reviewId}": {
      "schema": {
        "$ref": "#/entities/Review"
      },
      "description": "Client reviews for the tenant."
    }
  }
}
